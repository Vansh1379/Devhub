DATABASE_URL="postgresql://user:password@localhost:5432/virtual_office"
JWT_SECRET="replace-with-strong-secret"
PORT=4000

# Optional: for Phase 5 video calls (Dyte). Get from https://dev.dyte.io/apikeys
# Option A: Copy the full "Authorization Header" and set:
# DYTE_AUTH_HEADER=Basic <base64(orgId:apiKey)>
# Option B: Set org ID and API key separately:
# DYTE_ORG_ID=
# DYTE_API_KEY=
# Preset name from Dyte portal (default: group_call):
# DYTE_PRESET_NAME=group_call

